// import * as cron from 'node-cron';
// import { createRoutineFolder } from './api/apiService';
// import { buildFolderRoutineName } from './utils'

// const CRON_SCHEDULE = '0 20 * * 0'; // Every Sunday at 20:00

// cron.schedule(CRON_SCHEDULE, async () => {
//     try {
//         const routineFolderName = buildFolderRoutineName();
//         createRoutineFolder(routineFolderName)
//     } catch (error) {
//         console.error('Error en la tarea cron:', error);
//     }
// });
